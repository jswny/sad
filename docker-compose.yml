version: '3'
services:
  echo:
    build: .
    image: "jswny/discord-bot-deploy:${TAG:-beta}"
    container_name: "discord-bot-deploy-${TAG:-beta}"
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
